server {
  listen 80;
  server_name _;

  location = / {
    return 302 /hochschule-burgenland-docusaurus-demo/;
  }

  location /hochschule-burgenland-docusaurus-demo/ {
    alias /usr/share/nginx/html/;
    index index.html;
    try_files $uri $uri/ $uri.html =404;
  }
}
